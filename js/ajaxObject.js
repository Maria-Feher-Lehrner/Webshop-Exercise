/*class ajaxObject {
    constructor() {
        this.$action = $()
        this.$categoriesOutput = $('categories-output')
        this.$productOutput = $('products-output')
    }

    initGui() {
        this.loadCategoryData()
    }

    loadCategoryData() {
        let url = "http://localhost/bb/Webshop/api/index.php?resource=types"
        $.ajax({
            url: url,
            method: 'GET',
        })
            .done((response) => {
                let categoriesObject = response.
            })
    }
}*/